# Pipeline write test

STATUS: TEST ONLY

This folder proves that Codex can create and save a handoff package where Claude Code expects it. It is not artwork and must never be integrated.

- Asset slug: `_pipeline-test`
- Runtime files: none
- Intended use: verify folder creation, saving, and discovery
